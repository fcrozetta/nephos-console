# Nephos API base URL the BFF calls (server-side only).
NEPHOS_API_URL=http://127.0.0.1:8099
# v1 single-admin auth.
NEPHOS_CONSOLE_ADMIN_PASSWORD=changeme
NEPHOS_CONSOLE_SESSION_SECRET=dev-only-change-me
